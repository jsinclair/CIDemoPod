use_frameworks!

platform :ios, '10.0'

target 'CIDemoPod_Example' do
  pod 'CIDemoPod', :path => '../'

  target 'CIDemoPod_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 2.2.0'
    pod 'Nimble', '~> 10.0.0'
  end
end
